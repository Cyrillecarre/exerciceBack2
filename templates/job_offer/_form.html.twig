{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-center col-5 mx-auto">
    <button class="btn btn-success" 
            style="color: white; text-decoration: none; display: block; width: 100%; height: 100%;">
            {{ ('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
